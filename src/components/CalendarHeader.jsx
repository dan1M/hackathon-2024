import React from 'react';

const CalendarHeader = ({ onViewChange }) => {
  return (
    <div style={{ display: 'flex', justifyContent: 'space-between', margin: '10px 0' }}>
      <tr>Promotion info</tr>

    </div>
  );
};

export default CalendarHeader;
